name: Video Insight checks

on:
  push:
    paths:
      - "AI-Video-Insight-Lab/**"
      - ".github/workflows/video-insight-tests.yml"
  pull_request:
    paths:
      - "AI-Video-Insight-Lab/**"
      - ".github/workflows/video-insight-tests.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: AI-Video-Insight-Lab
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install -r backend/requirements.txt
      - run: cd backend && python -m unittest discover -s tests -v
